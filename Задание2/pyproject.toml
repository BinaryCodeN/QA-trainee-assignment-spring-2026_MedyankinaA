[tool.black]
line-length = 100
target-version = ["py39", "py310", "py311"]
include = '\.pyi?$'
exclude = '''
/(
    \.git
  | __pycache__
  | allure-results
  | allure-report
)/
'''

[tool.isort]
profile = "black"
line_length = 100
known_third_party = ["requests", "pytest", "allure"]
